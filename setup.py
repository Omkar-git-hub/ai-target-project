from setuptools import setup

setup(
    name='project',
    version='1.0',
    packages=['src', 'src.utils', 'tests'],
    install_requires=[],
    author='Author',
    author_email='author@example.com'
)